version: '3.8'

services:
  web:
    build:
      context: ./nbabets
      dockerfile: Dockerfile
      args:
        - API_URL=${API_URL}
    environment:
      - API_URL=${API_URL}
    ports:
      - "80:80"
